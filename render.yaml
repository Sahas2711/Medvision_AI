services:
  - type: web
    name: aarogya-drishti
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python backend/app_deploy.py"
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.11.9